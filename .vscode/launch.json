{
  "version": "0.2.0",
  "configurations": [
    
    {
      "name": "Debug current CMake target (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}"
    }
  ]
}
